services:
  db:
    image: postgres:18
    container_name: triviagame-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: trivia
      POSTGRES_PASSWORD: trivia
      POSTGRES_DB: trivia
    volumes:
      - pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trivia -d trivia"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped
  app:
    # LOCAL BUILD (default): `docker compose up --build` builds the image from ./app
    build:
      context: ./app
      dockerfile: Dockerfile
    image: triviaforge:local

    # DOCKER HUB: to run the published image instead, comment out the build section
    # above and this image line, then uncomment the line below
    #image: emancodetemplar/triviaforge:latest

    container_name: triviagame-app
    ports:
      - "${APP_PORT:-3000}:3000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Environment variables with sensible defaults
      # Can be overridden via shell environment or Docker Desktop UI
      APP_PORT: 3000
      NODE_ENV: ${NODE_ENV:-production}
      DEBUG_MODE: ${DEBUG_MODE:-false}
      # Guest-only mode: players join with just a display name (no usernames or accounts)
      GUEST_ONLY_MODE: ${GUEST_ONLY_MODE:-false}
      VERBOSE_LOGGING: ${VERBOSE_LOGGING:-false}
      DATABASE_URL: postgres://trivia:trivia@db:5432/trivia
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-changeme}
      CSRF_SECRET: ${CSRF_SECRET:-default-csrf-secret-change-in-production}
      TZ: ${TZ:-America/New_York}
      # Server URL for QR codes (can also be set in Admin > Settings)
      SERVER_URL: ${SERVER_URL:-}
      HOST_IP: ${HOST_IP:-}
      SESSION_TIMEOUT: ${SESSION_TIMEOUT:-3600000}
      APP_NAME: ${APP_NAME:-TriviaForge}
      # Socket.IO Rate Limiting (v5.5.0)
      # Increase limits for venues with many players on shared IP (NAT)
      SOCKET_RATE_WINDOW_MS: ${SOCKET_RATE_WINDOW_MS:-60000}
      SOCKET_JOIN_LIMIT: ${SOCKET_JOIN_LIMIT:-50}
      SOCKET_ANSWER_LIMIT: ${SOCKET_ANSWER_LIMIT:-300}
    volumes:
      - uploads:/app/public/uploads
      - backups:/app/backups
    restart: unless-stopped
volumes:
  pgdata:
  uploads:
  backups: